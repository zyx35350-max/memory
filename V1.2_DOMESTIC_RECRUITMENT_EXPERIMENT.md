# Match Me Clever V1.2 — Domestic Recruitment Platform Experimental Track

> 建立日期：2026-09-23
> 状态：Accepted / Experimental
> 适用平台：BOSS直聘、前程无忧（51Job）、智联招聘、猎聘
> 目的：先让 Match Me Clever 真正帮助用户搜索和读取国内招聘职位，再继续 Public Sources / Company ATS 两个分支。

---

## 1. 明确项目决策

**国内招聘软件必须作为 V1.2 的第一实验对象。**

本项目当前处于前期实验阶段。目标不是先等一个所谓“官方 API”再开始，而是：

1. 尝试使用公开职位页面；
2. 在浏览器可访问的情况下进行页面读取/结构化提取；
3. 必要时使用用户正常浏览器会话完成页面访问；
4. 验证真实搜索、分页、职位详情、Job ID、Company ID、Lifecycle 等能力；
5. 成功的数据进入现有 RawJob → Job Understanding → Career Engine；
6. Source 被 challenge / blocked 时，记录真实状态并回退到其他来源或 User Import。

**不把“等待官方 API”作为国内招聘平台实验的前置条件。**

长期稳定来源（Official API / Licensed Provider / Company ATS 等）属于后续稳定化方向，不阻塞当前实验。

---

## 2. 第一实验分支

### Domestic Apps

- BOSS直聘
- 51Job / 前程无忧
- 智联招聘
- 猎聘
- 后续再加入 Lagou

实验顺序：

`51Job → BOSS → Zhaopin → Liepin → Lagou`

但实际开发可以根据哪个平台当前更容易获得真实职位数据动态调整。

---

## 3. 当前发现的可参考爬虫

### 统一四平台参考实现

GitHub：RAYNLIU2005/-Multi-threadedCrawler

仓库明确包含：

`scraper/source/boss.py`
`scraper/source/job51.py`
`scraper/source/zhilian.py`
`scraper/source/liepin.py`

该项目 README 明确说明已支持 Boss、51Job、智联、猎聘，并采用 DrissionPage/Chromium、Source 分层、分页、登录态持久化、CSV/MySQL sink 等结构。

这些代码只能作为**研究和实验参考**，不能直接假定今天仍然可以运行。平台页面和风控机制变化后必须重新验证。

---

## 4. 平台专项参考

### 51Job

参考：
- RAYNLIU2005/-Multi-threadedCrawler/scraper/source/job51.py
- chenjiandongx/51job-spider
- JaapTeam/51job
- HowName/51job

当前最有价值的参考是统一项目中的 `job51.py`，因为它使用浏览器页面读取而不是依赖旧版静态搜索 URL。

### BOSS直聘

参考：
- RAYNLIU2005/-Multi-threadedCrawler/scraper/source/boss.py
- LiShui666/boss_jobs_spider（2026 版本）
- browser-use/browser-harness 的 BOSS job-search field test

当前公开研究表明 BOSS 页面/内部请求具有较强的会话与风控依赖，因此实验重点是**正常浏览器会话下的真实页面读取**，而不是把旧 Cookie、旧 token 或绕过风控的方法写死到 Match Me Clever。

### 智联招聘

参考：
- RAYNLIU2005/-Multi-threadedCrawler/scraper/source/zhilian.py
- Wuliu4250/ZhaopinData（2026-01 数据样本/代码）
- blackyau/zhaopin

其中 Wuliu4250 的实现使用 Selenium + Chrome、人工登录、自动翻页、进入详情页和 CSV 输出，适合作为当前浏览器实验参考。

### 猎聘

参考：
- RAYNLIU2005/-Multi-threadedCrawler/scraper/source/liepin.py
- rockbenben/ai-job-search-cn 的 liepin-search 技术文档

其中 rockbenben 项目的文档标注其 2026-07-23 的真实请求/fixture 实测，可作为当前猎聘搜索结构变化的研究参考。

---

## 5. 我们自己的 Adapter 不复制外部爬虫

外部爬虫的作用是：

> 学习当前页面结构、字段、分页、登录和挑战状态。

Match Me Clever 最终只实现自己的 Source Adapter：

`51JobAdapter`
`BossAdapter`
`ZhaopinAdapter`
`LiepinAdapter`

统一输出：

`FetchResult → RawJob`

禁止把外部项目的数据库、业务逻辑或平台专用评分逻辑直接搬进 Match Me Clever。

---

## 6. 实验成功标准

每个平台至少验证：

- Search
- City filter
- Keyword filter
- Job list
- Job detail
- Stable Job ID（如果页面/数据提供）
- Company ID（如果页面/数据提供）
- Source URL
- Salary
- Location
- Experience
- Education
- Description
- Pagination
- Published / Updated / Refreshed signal（如果存在）
- Challenge detection
- True zero result 与 challenge 的区分

第一阶段不要求一次做到全量采集。

**能让用户输入关键词 + 城市，然后在自己的 Match Me Clever 中看到真实职位，就是第一阶段有价值的成功。**

---

## 7. Challenge / Blocked 的处理

如果平台返回：

- CAPTCHA
- Slider
- Access Verification
- Login required
- 403 / 429
- 空页面但存在明显 challenge

必须记录为：

`challenged`
或
`blocked`
或
`unauthorized`

不能转换成：

`success + 0 jobs`

也不能通过绕过 CAPTCHA、绕过登录限制、窃取/导出 Cookie 等方式解决。

如果用户本人正常登录后可以继续浏览，则可以把“用户正常浏览器会话”作为实验条件，并重新验证。

---

## 8. 关于“官方 API”的项目定位

**Official API 不是当前国内招聘平台实验的前置条件。**

本项目需要明确区分：

- Experimental access：当前为了个人求职实际使用而进行的页面/浏览器读取实验
- Stable source：未来可以长期维护的官方 API、Licensed Provider、Company ATS、稳定公开接口

因此：

> 先做能用的实验性 Source Adapter，再决定哪些来源值得进入长期稳定化。

不要因为没有官方 API 就停止 Branch 1。

---

## 9. 与另外两个分支的关系

当前只推进：

`Branch 1 — Domestic Apps / Personal Job Search Experiment`

完成第一轮真实职位获取后，再继续：

`Branch 2 — Public Sources`

以及：

`Branch 3 — Company ATS`

Company Intelligence / Portfolio 继续保持后续阶段，不提前打断真实求职实验。

---

## 10. 当前下一步

第一件实际开发任务：

**51JobAdapter**

目标：

`51Job real search/page
→ read
→ parse
→ RawJob
→ existing Job Understanding
→ existing Career Engine
→ user sees real jobs
`

如果 51Job 当前访问被验证页拦截，则记录真实结果并保留 User Import fallback；不把失败解释为“没有职位”。

之后再按同样方式推进 BOSS、智联、猎聘。

